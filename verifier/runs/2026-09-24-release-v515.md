# Run — Release v5.15 : routes ramifiées (2026-09-24)

Environnement : Chromium 1194 (Playwright 1.56, SwiftShader), bac à sable **sans accès réseau externe** (Supabase injoignable).

## Nouvelle suite — `tests/e2e/routes15.cjs`

- Commande : `NODE_PATH=<node_modules globaux> node tests/e2e/routes15.cjs .`
- Résultat : **28/28 PASS × 3 runs consécutifs** (exit 0)
- Contexte tactile iPhone 14 (390×844, DPR 3, `hasTouch`) : tous les choix faits par `page.tap()` ; un test iPhone SE (375×667).
- Parcours couvert : sauvegarde v5.14 préexistante → menu → Campagne (tap) → combat → boss vague 3 → bifurcation (3 cartes risque/récompense) → Forge solaire (tap + « Engager ») → modificateur mesuré (vitesse ×1,2, cadence ×1,35, pièces ×2) → point de reprise → rechargement → « Reprendre la route » → état et modificateur restaurés → Vide profond (clavier 1-3 + Entrée ; obscurité mesurée sur le canvas ; +1 vie en sortie) → Anomalie quantique (projectiles déviés ; prototypes rares) → vague 12 (Signal jamais proposé avant le boss final) → boss final → victoire + récap des routes → `meta.routes` → 2ᵉ partie : Signal inconnu → boss vague 6 = Écho de la Prime → butin + archive.
- Déterminisme : même `?seed=` → mêmes offres sur 2 sessions ; Opération du jour → seed daté, `Math.random` non consommé, pas de point de reprise.
- PWA : `sw.js` VERSION `np4-v5.15` (purge du cache v5.14), module intégré à `index.html` (aucun nouveau fichier requis hors ligne).

## Non-régression

| Suite | v5.14 (réf.) | v5.15 |
|---|---|---|
| `smoke13.cjs` | 20/20 | **20/20** |
| `run.cjs` | 86–87 PASS · 3–4 FAIL · 8 manuels | **86 PASS · 4 FAIL · 8 manuels** |

Échecs communs aux deux versions :
- TC-LB-001, TC-LB-002b, TC-TOUR-003 : classement / tournoi Supabase (réseau indisponible dans le bac à sable).
- TC-V12-005 (intermittent, observé aussi sur la référence) : la téléportation de l'Architecte est aléatoire et testée par un seuil de 20 px en 500 ms ; Acte II, aucune route active.

Adaptations du harnais `run.cjs` :
- `require('playwright')` résolu via `NODE_PATH` (repli sur l'ancien chemin absolu).
- TC-SEC-001..003 : marqués *manuels* au lieu d'un arrêt fatal quand Supabase est injoignable.
- TC-BOOT-005 : `serviceWorkers: 'block'` (le SW v5.13.2 préchargeait les assets hors de portée de `ctx.route`).
- Un « pilote » choisit automatiquement la première route quand l'état vaut `route` : les parcours historiques (boss final, game over, bullet-time, fantôme) tuent des boss et attendaient `playing`.
